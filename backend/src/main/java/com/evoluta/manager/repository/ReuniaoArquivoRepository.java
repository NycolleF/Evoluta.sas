package com.evoluta.manager.repository;

import com.evoluta.manager.model.ReuniaoArquivo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReuniaoArquivoRepository extends JpaRepository<ReuniaoArquivo, Integer> {
}
